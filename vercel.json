{
  "version": 2,
  "installCommand": "pip install --break-system-packages -r requirements.txt && python myproject/manage.py migrate --noinput && python myproject/manage.py collectstatic --noinput",
  "builds": [
    {
      "src": "api/index.py",
      "use": "@vercel/python",
      "config": {
        "runtime": "python3.12"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.py"
    }
  ]
}
